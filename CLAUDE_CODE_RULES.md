# 🚨 **CLAUDE CODE 強制性開發規則**

## ⚠️ **違反規則的嚴重後果**

違反這些規則將導致：
- **分析錯誤**：基於錯誤推測導致用戶體驗問題
- **資源浪費**：忽略現有工具造成開發效率低下  
- **品質問題**：程式碼品質不符合專案標準
- **知識流失**：未記錄錯誤教訓導致重複犯錯

---

## 📖 **必讀規則 - 每次對話開始前**

### 1. 🧪 **實際測試優於程式碼推測**
**強制性規則**: 任何功能分析都必須通過實際測試驗證

```bash
# ❌ 禁止：基於推測分析
"這個功能應該可以工作..."

# ✅ 必須：實際測試驗證
"創建 Playwright 測試驗證實際行為"
```

### 2. 🧠 **超級思考系統性調試方法論**
**當遇到複雜問題時，必須使用四階段系統性調試**：

#### **Phase 1: 數據輸入測試**
- 使用非預設數據建立測試案例
- 監控 API 請求和回應
- 記錄實際輸入的數據

#### **Phase 2: 數據驗證**  
- 比對輸入數據 vs 儲存數據 vs 顯示數據
- 識別數據流程中的斷點
- 記錄數據不一致性

#### **Phase 3: 問題識別**
- 分類問題：輸入問題、輸出問題、功能問題
- 通過實際測試確認根本原因
- 記錄具體失敗點

#### **Phase 4: 知識記錄**
- 將調試方法和結論記錄到知識庫
- 建立可重複使用的調試模式
- 更新防錯檢查清單

**實際應用範例**：
```javascript
// 系統性調試實例
test('超級思考調試', async ({ page }) => {
  // Phase 1: 使用非預設數據測試
  const testData = {
    customer: 'Final Test Customer', // 非第一個選項
    status: 'sent',                 // 非預設 draft
    amount: 150.00                  // 非預設金額
  };
  
  // Phase 2: 追蹤數據流
  await inputData(testData);
  const storedData = await checkDatabase();
  const displayedData = await checkUI();
  
  // Phase 3: 比對數據一致性
  expect(testData).toEqual(storedData);
  expect(storedData).toEqual(displayedData);
});
```

### 3. 🔧 **善用現有工具**
**強制性規則**: 優先使用專案配置的 MCP 工具和測試資源

- ✅ **優先使用**: Playwright MCP 進行端到端測試
- ✅ **優先使用**: Task Master AI 進行任務管理
- ✅ **優先使用**: 專案現有的測試框架和工具
- ❌ **禁止**: 重複創造輪子或忽略現有工具

### 4. 📝 **基於事實報告**
**強制性規則**: 所有評估必須有具體證據支持

```markdown
# ❌ 禁止：無證據推測
"這個功能可能有問題"

# ✅ 必須：具體證據報告
"Playwright 測試顯示小計 $0.00 但列表頁顯示 $100.00，
 證據：截圖 screenshot.png，
 根本原因：show.blade.php 第120行欄位名稱錯誤"
```

### 5. 🔄 **記錄錯誤教訓**
**強制性規則**: 犯錯時立即記錄到知識庫

**Bug 記錄模板**：
```markdown
# Bug 記錄 - <問題簡述>

## 🐛 問題描述
[具體問題症狀]

## 🔍 根本原因
[通過系統性調試發現的真正原因]

## 🛠️ 解決方案
[具體修復步驟和程式碼變更]

## 🧪 驗證方法
[如何測試確認修復成功]

## 📚 學習要點
[避免重複錯誤的知識點]
```

---

## 🔬 **系統性調試檢查清單**

### ✅ **調試前檢查**
- [ ] 已讀取 CLAUDE_CODE_RULES.md
- [ ] 已檢查專案知識庫 (memory-bank/)
- [ ] 已確認現有工具和測試資源
- [ ] 已準備使用四階段調試法

### ✅ **調試過程檢查**
- [ ] 使用非預設數據測試
- [ ] 監控 API 請求和回應
- [ ] 比對輸入、儲存、顯示數據
- [ ] 基於實際測試而非推測下結論

### ✅ **調試後檢查**
- [ ] 問題根本原因已確認
- [ ] 解決方案已實際驗證
- [ ] Bug 記錄已建立
- [ ] 知識庫已更新
- [ ] 預防措施已實施

---

## 📋 **常見問題模式和解決方案**

### 🎯 **模式1: 數據顯示不一致**
**症狀**: 列表頁和詳情頁顯示不同數據
**調試方法**: 使用 Phase 2 數據驗證
**常見原因**: 欄位名稱映射錯誤
**解決模式**: 數據標準化處理

### 🎯 **模式2: 表單數據不填充**
**症狀**: 編輯表單空白
**調試方法**: 使用 Phase 1 輸入測試
**常見原因**: 控制器未傳遞數據或欄位名稱不符
**解決模式**: 數據綁定檢查

### 🎯 **模式3: 搜尋功能無效**
**症狀**: 搜尋條件不過濾結果
**調試方法**: 使用 Phase 3 功能識別
**常見原因**: 前端參數未傳遞或後端未處理
**解決模式**: 參數追蹤驗證

---

## 🚀 **最佳實踐範例**

### 🏆 **優秀調試範例**
```javascript
// 超級思考系統性調試
test('問題診斷：報價單數據顯示', async ({ page }) => {
  // 1. 輸入非預設數據
  const inputData = { customer: 'Test', amount: 100 };
  await createQuote(inputData);
  
  // 2. 驗證數據一致性
  const listData = await getListDisplay();
  const detailData = await getDetailDisplay();
  
  // 3. 識別具體問題
  if (listData.amount !== detailData.amount) {
    throw new Error(`數據不一致: 列表${listData.amount} vs 詳情${detailData.amount}`);
  }
  
  // 4. 記錄調試結果
  console.log('✅ 數據一致性驗證通過');
});
```

### 🏆 **優秀 Bug 記錄範例**
```markdown
# Bug 記錄 - 報價單詳情頁金額顯示錯誤

## 🔍 系統性調試結果
- **Phase 1**: 輸入數據正常 ($100.00)
- **Phase 2**: 列表頁顯示正常 ($100.00)
- **Phase 3**: 詳情頁顯示錯誤 ($0.00) ← 問題定位
- **Phase 4**: 根本原因是欄位名稱錯誤

## 🛠️ 解決方案
修改 `show.blade.php` 第120行：
- 錯誤：`$quote['discount_amount']`
- 正確：`$quote['discount']`

## 🧪 驗證方法
Playwright 測試確認修復後顯示正確金額

## 📚 學習要點
使用系統性調試避免基於推測的錯誤分析
```

---

## 🔒 **違規處罰機制**

### ⚠️ **一級違規**（推測性分析）
- 立即停止當前分析
- 重新使用實際測試方法
- 記錄違規教訓

### 🚨 **二級違規**（忽略現有工具）
- 重新檢查專案工具清單
- 使用正確的工具重新執行
- 更新工具使用知識

### 💥 **三級違規**（未記錄錯誤教訓）
- 立即建立完整 Bug 記錄
- 更新知識庫和檢查清單
- 建立預防措施

---

**💡 記住：實際測試永遠勝過程式碼推測！**
**🎯 目標：建立可驗證、可重複、可學習的開發流程！**

---
*最後更新: 2025-08-07*
*系統性調試方法論: 超級思考四階段法*