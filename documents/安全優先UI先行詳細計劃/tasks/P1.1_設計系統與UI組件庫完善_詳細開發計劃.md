# P1.1 設計系統與UI組件庫完善 - 詳細開發計劃

**優先級**: 🔥 P1 (高優先級)  
**預估時間**: 2週 (10個工作天)  
**前置依賴**: 無  
**責任範圍**: 建立完整的設計系統基礎架構

---

## 🎯 **開發目標**

### **主要目標**
- 建立完整的設計令牌系統 (Design Tokens)
- 實作 50+ 可重用UI組件
- 建立主題切換機制
- 提供完整的組件文檔

### **成功指標**
- ✅ 設計令牌涵蓋顏色、字體、間距、陰影、邊框
- ✅ 50+ UI組件完整實作且可重用
- ✅ 明暗主題無縫切換
- ✅ 每個組件都有文檔和使用範例
- ✅ 響應式設計在所有設備正常運作

---

## 📋 **詳細開發步驟**

### **第一天：設計令牌系統建立**

#### **上午 (4小時)**：核心設計令牌
```php
// 檔案: frontend/resources/css/design-tokens.css

/* 建立完整的設計令牌變數 */
:root {
  /* 顏色系統 */
  --nexus-primary-50: #eff6ff;
  --nexus-primary-100: #dbeafe;
  --nexus-primary-200: #bfdbfe;
  --nexus-primary-300: #93c5fd;
  --nexus-primary-400: #60a5fa;
  --nexus-primary-500: #3b82f6;  /* 主色 */
  --nexus-primary-600: #2563eb;
  --nexus-primary-700: #1d4ed8;
  --nexus-primary-800: #1e40af;
  --nexus-primary-900: #1e3a8a;
  
  /* 中性色 */
  --nexus-gray-50: #f9fafb;
  --nexus-gray-100: #f3f4f6;
  --nexus-gray-200: #e5e7eb;
  --nexus-gray-300: #d1d5db;
  --nexus-gray-400: #9ca3af;
  --nexus-gray-500: #6b7280;
  --nexus-gray-600: #4b5563;
  --nexus-gray-700: #374151;
  --nexus-gray-800: #1f2937;
  --nexus-gray-900: #111827;
  
  /* 語義化顏色 */
  --nexus-success: #10b981;
  --nexus-warning: #f59e0b;
  --nexus-error: #ef4444;
  --nexus-info: #3b82f6;
  
  /* 字體系統 */
  --nexus-font-family-sans: 'Inter', 'Noto Sans TC', system-ui, sans-serif;
  --nexus-font-family-mono: 'JetBrains Mono', 'Courier New', monospace;
  
  /* 字體大小 */
  --nexus-text-xs: 0.75rem;    /* 12px */
  --nexus-text-sm: 0.875rem;   /* 14px */
  --nexus-text-base: 1rem;     /* 16px */
  --nexus-text-lg: 1.125rem;   /* 18px */
  --nexus-text-xl: 1.25rem;    /* 20px */
  --nexus-text-2xl: 1.5rem;    /* 24px */
  --nexus-text-3xl: 1.875rem;  /* 30px */
  --nexus-text-4xl: 2.25rem;   /* 36px */
  
  /* 間距系統 */
  --nexus-space-0: 0;
  --nexus-space-1: 0.25rem;    /* 4px */
  --nexus-space-2: 0.5rem;     /* 8px */
  --nexus-space-3: 0.75rem;    /* 12px */
  --nexus-space-4: 1rem;       /* 16px */
  --nexus-space-5: 1.25rem;    /* 20px */
  --nexus-space-6: 1.5rem;     /* 24px */
  --nexus-space-8: 2rem;       /* 32px */
  --nexus-space-10: 2.5rem;    /* 40px */
  --nexus-space-12: 3rem;      /* 48px */
  --nexus-space-16: 4rem;      /* 64px */
  
  /* 陰影系統 */
  --nexus-shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
  --nexus-shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  --nexus-shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
  --nexus-shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
  
  /* 邊框半徑 */
  --nexus-radius-sm: 0.125rem;   /* 2px */
  --nexus-radius-md: 0.375rem;   /* 6px */
  --nexus-radius-lg: 0.5rem;     /* 8px */
  --nexus-radius-xl: 0.75rem;    /* 12px */
  --nexus-radius-full: 9999px;
  
  /* 動畫時間 */
  --nexus-transition-fast: 150ms;
  --nexus-transition-normal: 300ms;
  --nexus-transition-slow: 500ms;
}

/* 暗色主題 */
[data-theme="dark"] {
  --nexus-bg-primary: var(--nexus-gray-900);
  --nexus-bg-secondary: var(--nexus-gray-800);
  --nexus-bg-tertiary: var(--nexus-gray-700);
  --nexus-text-primary: var(--nexus-gray-100);
  --nexus-text-secondary: var(--nexus-gray-300);
  --nexus-text-muted: var(--nexus-gray-500);
  --nexus-border-primary: var(--nexus-gray-700);
}

/* 亮色主題 */
[data-theme="light"] {
  --nexus-bg-primary: #ffffff;
  --nexus-bg-secondary: var(--nexus-gray-50);
  --nexus-bg-tertiary: var(--nexus-gray-100);
  --nexus-text-primary: var(--nexus-gray-900);
  --nexus-text-secondary: var(--nexus-gray-700);
  --nexus-text-muted: var(--nexus-gray-500);
  --nexus-border-primary: var(--nexus-gray-200);
}
```

#### **下午 (4小時)**：基礎工具類與Tailwind配置
```javascript
// 檔案: frontend/tailwind.config.js

module.exports = {
  content: [
    './resources/views/**/*.blade.php',
    './resources/js/**/*.vue',
    './resources/js/**/*.js',
  ],
  darkMode: ['class', '[data-theme="dark"]'],
  theme: {
    extend: {
      colors: {
        nexus: {
          primary: {
            50: 'var(--nexus-primary-50)',
            100: 'var(--nexus-primary-100)',
            200: 'var(--nexus-primary-200)',
            300: 'var(--nexus-primary-300)',
            400: 'var(--nexus-primary-400)',
            500: 'var(--nexus-primary-500)',
            600: 'var(--nexus-primary-600)',
            700: 'var(--nexus-primary-700)',
            800: 'var(--nexus-primary-800)',
            900: 'var(--nexus-primary-900)',
          },
          gray: {
            50: 'var(--nexus-gray-50)',
            100: 'var(--nexus-gray-100)',
            200: 'var(--nexus-gray-200)',
            300: 'var(--nexus-gray-300)',
            400: 'var(--nexus-gray-400)',
            500: 'var(--nexus-gray-500)',
            600: 'var(--nexus-gray-600)',
            700: 'var(--nexus-gray-700)',
            800: 'var(--nexus-gray-800)',
            900: 'var(--nexus-gray-900)',
          }
        }
      },
      fontFamily: {
        sans: ['var(--nexus-font-family-sans)'],
        mono: ['var(--nexus-font-family-mono)'],
      },
      spacing: {
        'nexus-1': 'var(--nexus-space-1)',
        'nexus-2': 'var(--nexus-space-2)',
        'nexus-3': 'var(--nexus-space-3)',
        'nexus-4': 'var(--nexus-space-4)',
        'nexus-5': 'var(--nexus-space-5)',
        'nexus-6': 'var(--nexus-space-6)',
        'nexus-8': 'var(--nexus-space-8)',
        'nexus-10': 'var(--nexus-space-10)',
        'nexus-12': 'var(--nexus-space-12)',
        'nexus-16': 'var(--nexus-space-16)',
      },
      boxShadow: {
        'nexus-sm': 'var(--nexus-shadow-sm)',
        'nexus-md': 'var(--nexus-shadow-md)',
        'nexus-lg': 'var(--nexus-shadow-lg)',
        'nexus-xl': 'var(--nexus-shadow-xl)',
      },
      borderRadius: {
        'nexus-sm': 'var(--nexus-radius-sm)',
        'nexus-md': 'var(--nexus-radius-md)',
        'nexus-lg': 'var(--nexus-radius-lg)',
        'nexus-xl': 'var(--nexus-radius-xl)',
      },
      transitionDuration: {
        'nexus-fast': 'var(--nexus-transition-fast)',
        'nexus-normal': 'var(--nexus-transition-normal)',
        'nexus-slow': 'var(--nexus-transition-slow)',
      }
    }
  }
}
```

### **第二天：按鈕與表單組件**

#### **上午 (4小時)**：按鈕組件系統
```php
<?php
// 檔案: frontend/resources/views/components/ui/button.blade.php

@props([
    'variant' => 'primary',    // primary, secondary, outline, ghost, danger
    'size' => 'md',           // xs, sm, md, lg, xl
    'disabled' => false,
    'loading' => false,
    'icon' => null,
    'iconPosition' => 'left', // left, right
    'fullWidth' => false,
    'href' => null,
    'type' => 'button'
])

@php
$baseClasses = 'inline-flex items-center justify-center font-medium transition-all duration-nexus-normal focus:outline-none focus:ring-2 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed';

$variantClasses = [
    'primary' => 'bg-nexus-primary-500 text-white hover:bg-nexus-primary-600 focus:ring-nexus-primary-500 shadow-nexus-sm',
    'secondary' => 'bg-nexus-gray-100 text-nexus-gray-900 hover:bg-nexus-gray-200 focus:ring-nexus-gray-500 border border-nexus-gray-300',
    'outline' => 'border border-nexus-primary-500 text-nexus-primary-500 hover:bg-nexus-primary-50 focus:ring-nexus-primary-500',
    'ghost' => 'text-nexus-primary-500 hover:bg-nexus-primary-50 focus:ring-nexus-primary-500',
    'danger' => 'bg-red-500 text-white hover:bg-red-600 focus:ring-red-500 shadow-nexus-sm',
];

$sizeClasses = [
    'xs' => 'px-2 py-1 text-nexus-xs rounded-nexus-sm',
    'sm' => 'px-3 py-1.5 text-nexus-sm rounded-nexus-md',
    'md' => 'px-4 py-2 text-nexus-base rounded-nexus-md',
    'lg' => 'px-6 py-3 text-nexus-lg rounded-nexus-lg',
    'xl' => 'px-8 py-4 text-nexus-xl rounded-nexus-lg',
];

$classes = $baseClasses . ' ' . $variantClasses[$variant] . ' ' . $sizeClasses[$size];

if ($fullWidth) {
    $classes .= ' w-full';
}

if ($loading || $disabled) {
    $classes .= ' pointer-events-none';
}

$tag = $href ? 'a' : 'button';
$tagAttributes = $href ? ['href' => $href] : ['type' => $type];
@endphp

<{{ $tag }} 
    {{ $attributes->merge(array_merge($tagAttributes, ['class' => $classes])) }}
    @if($disabled && !$href) disabled @endif
>
    @if($loading)
        <svg class="animate-spin -ml-1 mr-2 h-4 w-4 text-current" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
        </svg>
        處理中...
    @else
        @if($icon && $iconPosition === 'left')
            <x-dynamic-component :component="'heroicon-o-' . $icon" class="w-4 h-4 mr-2" />
        @endif
        
        {{ $slot }}
        
        @if($icon && $iconPosition === 'right')
            <x-dynamic-component :component="'heroicon-o-' . $icon" class="w-4 h-4 ml-2" />
        @endif
    @endif
</{{ $tag }}>
```

#### **下午 (4小時)**：輸入表單組件
```php
<?php
// 檔案: frontend/resources/views/components/ui/input.blade.php

@props([
    'label' => null,
    'name' => '',
    'type' => 'text',
    'placeholder' => '',
    'required' => false,
    'disabled' => false,
    'readonly' => false,
    'error' => null,
    'help' => null,
    'size' => 'md',        // sm, md, lg
    'prefix' => null,
    'suffix' => null,
    'icon' => null,
    'iconPosition' => 'left',
])

@php
$inputId = $name ?: 'input_' . uniqid();

$sizeClasses = [
    'sm' => 'px-3 py-1.5 text-nexus-sm',
    'md' => 'px-4 py-2 text-nexus-base',
    'lg' => 'px-4 py-3 text-nexus-lg',
];

$baseClasses = 'block w-full rounded-nexus-md border border-nexus-gray-300 bg-white text-nexus-gray-900 placeholder-nexus-gray-400 focus:border-nexus-primary-500 focus:ring-1 focus:ring-nexus-primary-500 disabled:bg-nexus-gray-50 disabled:text-nexus-gray-500 disabled:cursor-not-allowed transition-colors duration-nexus-fast';

$errorClasses = $error ? 'border-red-300 focus:border-red-500 focus:ring-red-500' : '';

$classes = $baseClasses . ' ' . $sizeClasses[$size] . ' ' . $errorClasses;

if ($prefix || $suffix || $icon) {
    $classes .= ' pl-10'; // 為圖標或前綴留空間
}
@endphp

<div class="w-full">
    @if($label)
        <label for="{{ $inputId }}" class="block text-nexus-sm font-medium text-nexus-gray-700 mb-1">
            {{ $label }}
            @if($required)
                <span class="text-red-500 ml-1">*</span>
            @endif
        </label>
    @endif
    
    <div class="relative">
        @if($prefix || $icon)
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                @if($icon)
                    <x-dynamic-component 
                        :component="'heroicon-o-' . $icon" 
                        class="h-5 w-5 text-nexus-gray-400" 
                    />
                @else
                    <span class="text-nexus-gray-500 text-nexus-sm">{{ $prefix }}</span>
                @endif
            </div>
        @endif
        
        <input
            id="{{ $inputId }}"
            name="{{ $name }}"
            type="{{ $type }}"
            placeholder="{{ $placeholder }}"
            @if($required) required @endif
            @if($disabled) disabled @endif
            @if($readonly) readonly @endif
            {{ $attributes->merge(['class' => $classes]) }}
        />
        
        @if($suffix)
            <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                <span class="text-nexus-gray-500 text-nexus-sm">{{ $suffix }}</span>
            </div>
        @endif
    </div>
    
    @if($error)
        <p class="mt-1 text-nexus-sm text-red-600" role="alert">{{ $error }}</p>
    @endif
    
    @if($help && !$error)
        <p class="mt-1 text-nexus-sm text-nexus-gray-500">{{ $help }}</p>
    @endif
</div>
```

### **第三天：選擇與導航組件**

#### **上午 (4小時)**：下拉選單與多選組件
```php
<?php
// 檔案: frontend/resources/views/components/ui/select.blade.php

@props([
    'label' => null,
    'name' => '',
    'placeholder' => '請選擇...',
    'options' => [],
    'selected' => null,
    'multiple' => false,
    'searchable' => false,
    'required' => false,
    'disabled' => false,
    'error' => null,
    'help' => null,
    'size' => 'md',
])

@php
$selectId = $name ?: 'select_' . uniqid();

$sizeClasses = [
    'sm' => 'px-3 py-1.5 text-nexus-sm',
    'md' => 'px-4 py-2 text-nexus-base',
    'lg' => 'px-4 py-3 text-nexus-lg',
];

$baseClasses = 'block w-full rounded-nexus-md border border-nexus-gray-300 bg-white text-nexus-gray-900 focus:border-nexus-primary-500 focus:ring-1 focus:ring-nexus-primary-500 disabled:bg-nexus-gray-50 disabled:text-nexus-gray-500 disabled:cursor-not-allowed transition-colors duration-nexus-fast';

$errorClasses = $error ? 'border-red-300 focus:border-red-500 focus:ring-red-500' : '';

$classes = $baseClasses . ' ' . $sizeClasses[$size] . ' ' . $errorClasses;
@endphp

<div class="w-full" @if($searchable) x-data="searchableSelect()" @endif>
    @if($label)
        <label for="{{ $selectId }}" class="block text-nexus-sm font-medium text-nexus-gray-700 mb-1">
            {{ $label }}
            @if($required)
                <span class="text-red-500 ml-1">*</span>
            @endif
        </label>
    @endif
    
    @if($searchable)
        <!-- 可搜尋下拉選單 -->
        <div class="relative">
            <input
                type="text"
                x-model="searchTerm"
                @click="isOpen = true"
                @keydown.escape="isOpen = false"
                placeholder="{{ $placeholder }}"
                class="{{ $classes }}"
                autocomplete="off"
            />
            
            <div class="absolute inset-y-0 right-0 flex items-center pr-2 pointer-events-none">
                <svg class="h-5 w-5 text-nexus-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
            </div>
            
            <div 
                x-show="isOpen" 
                x-transition:enter="transition ease-out duration-100"
                x-transition:enter-start="transform opacity-0 scale-95"
                x-transition:enter-end="transform opacity-100 scale-100"
                x-transition:leave="transition ease-in duration-75"
                x-transition:leave-start="transform opacity-100 scale-100"
                x-transition:leave-end="transform opacity-0 scale-95"
                @click.away="isOpen = false"
                class="absolute z-10 mt-1 w-full bg-white shadow-nexus-lg rounded-nexus-md border border-nexus-gray-200 max-h-60 overflow-auto"
            >
                <template x-for="option in filteredOptions" :key="option.value">
                    <div 
                        @click="selectOption(option)"
                        class="px-4 py-2 cursor-pointer hover:bg-nexus-gray-50 flex items-center justify-between"
                        :class="{'bg-nexus-primary-50 text-nexus-primary-700': selectedValue === option.value}"
                    >
                        <span x-text="option.label"></span>
                        <svg 
                            x-show="selectedValue === option.value" 
                            class="h-4 w-4 text-nexus-primary-500" 
                            fill="currentColor" 
                            viewBox="0 0 20 20"
                        >
                            <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                        </svg>
                    </div>
                </template>
                
                <div x-show="filteredOptions.length === 0" class="px-4 py-2 text-nexus-gray-500 text-center">
                    沒有找到相符的選項
                </div>
            </div>
            
            <input 
                type="hidden" 
                name="{{ $name }}" 
                x-model="selectedValue"
                @if($required) required @endif
            />
        </div>
    @else
        <!-- 一般下拉選單 -->
        <select
            id="{{ $selectId }}"
            name="{{ $name }}"
            @if($required) required @endif
            @if($disabled) disabled @endif
            @if($multiple) multiple @endif
            {{ $attributes->merge(['class' => $classes]) }}
        >
            @if(!$multiple && $placeholder)
                <option value="">{{ $placeholder }}</option>
            @endif
            
            @foreach($options as $value => $label)
                <option 
                    value="{{ $value }}" 
                    @if($selected == $value || (is_array($selected) && in_array($value, $selected))) selected @endif
                >
                    {{ $label }}
                </option>
            @endforeach
        </select>
    @endif
    
    @if($error)
        <p class="mt-1 text-nexus-sm text-red-600" role="alert">{{ $error }}</p>
    @endif
    
    @if($help && !$error)
        <p class="mt-1 text-nexus-sm text-nexus-gray-500">{{ $help }}</p>
    @endif
</div>

@if($searchable)
@push('scripts')
<script>
function searchableSelect() {
    return {
        isOpen: false,
        searchTerm: '',
        selectedValue: '{{ $selected }}',
        options: @json(collect($options)->map(fn($label, $value) => ['value' => $value, 'label' => $label])->values()),
        
        get filteredOptions() {
            if (!this.searchTerm) return this.options;
            
            return this.options.filter(option => 
                option.label.toLowerCase().includes(this.searchTerm.toLowerCase())
            );
        },
        
        selectOption(option) {
            this.selectedValue = option.value;
            this.searchTerm = option.label;
            this.isOpen = false;
        }
    }
}
</script>
@endpush
@endif
```

#### **下午 (4小時)**：導航與分頁組件
```php
<?php
// 檔案: frontend/resources/views/components/ui/pagination.blade.php

@props([
    'paginator',
    'simple' => false,
    'size' => 'md'  // sm, md, lg
])

@php
$sizeClasses = [
    'sm' => 'px-2 py-1 text-nexus-xs',
    'md' => 'px-3 py-2 text-nexus-sm',
    'lg' => 'px-4 py-2 text-nexus-base',
];

$buttonSize = $sizeClasses[$size];
@endphp

@if ($paginator->hasPages())
    <nav class="flex items-center justify-between" aria-label="分頁導航">
        @if(!$simple)
            <!-- 詳細資訊 -->
            <div class="hidden sm:flex-1 sm:flex sm:items-center sm:justify-between">
                <div>
                    <p class="text-nexus-sm text-nexus-gray-700">
                        顯示第
                        <span class="font-medium">{{ $paginator->firstItem() ?: 0 }}</span>
                        到
                        <span class="font-medium">{{ $paginator->lastItem() ?: 0 }}</span>
                        筆，共
                        <span class="font-medium">{{ $paginator->total() }}</span>
                        筆結果
                    </p>
                </div>
                
                <div>
                    <span class="relative z-0 inline-flex rounded-nexus-md shadow-nexus-sm -space-x-px">
                        {{-- 上一頁 --}}
                        @if ($paginator->onFirstPage())
                            <span class="relative inline-flex items-center {{ $buttonSize }} rounded-l-nexus-md border border-nexus-gray-300 bg-white text-nexus-gray-500 cursor-not-allowed">
                                <svg class="h-4 w-4" fill="currentColor" viewBox="0 0 20 20">
                                    <path fill-rule="evenodd" d="M12.707 5.293a1 1 0 010 1.414L9.414 10l3.293 3.293a1 1 0 01-1.414 1.414l-4-4a1 1 0 010-1.414l4-4a1 1 0 011.414 0z" clip-rule="evenodd" />
                                </svg>
                            </span>
                        @else
                            <a href="{{ $paginator->previousPageUrl() }}" 
                               class="relative inline-flex items-center {{ $buttonSize }} rounded-l-nexus-md border border-nexus-gray-300 bg-white text-nexus-gray-500 hover:bg-nexus-gray-50 transition-colors">
                                <svg class="h-4 w-4" fill="currentColor" viewBox="0 0 20 20">
                                    <path fill-rule="evenodd" d="M12.707 5.293a1 1 0 010 1.414L9.414 10l3.293 3.293a1 1 0 01-1.414 1.414l-4-4a1 1 0 010-1.414l4-4a1 1 0 011.414 0z" clip-rule="evenodd" />
                                </svg>
                            </a>
                        @endif

                        {{-- 頁碼 --}}
                        @foreach ($paginator->getUrlRange(1, $paginator->lastPage()) as $page => $url)
                            @if ($page == $paginator->currentPage())
                                <span class="relative inline-flex items-center {{ $buttonSize }} border border-nexus-primary-500 bg-nexus-primary-50 text-nexus-primary-600 font-medium">
                                    {{ $page }}
                                </span>
                            @else
                                <a href="{{ $url }}" 
                                   class="relative inline-flex items-center {{ $buttonSize }} border border-nexus-gray-300 bg-white text-nexus-gray-500 hover:bg-nexus-gray-50 transition-colors">
                                    {{ $page }}
                                </a>
                            @endif
                        @endforeach

                        {{-- 下一頁 --}}
                        @if ($paginator->hasMorePages())
                            <a href="{{ $paginator->nextPageUrl() }}" 
                               class="relative inline-flex items-center {{ $buttonSize }} rounded-r-nexus-md border border-nexus-gray-300 bg-white text-nexus-gray-500 hover:bg-nexus-gray-50 transition-colors">
                                <svg class="h-4 w-4" fill="currentColor" viewBox="0 0 20 20">
                                    <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                                </svg>
                            </a>
                        @else
                            <span class="relative inline-flex items-center {{ $buttonSize }} rounded-r-nexus-md border border-nexus-gray-300 bg-white text-nexus-gray-500 cursor-not-allowed">
                                <svg class="h-4 w-4" fill="currentColor" viewBox="0 0 20 20">
                                    <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                                </svg>
                            </span>
                        @endif
                    </span>
                </div>
            </div>
        @endif

        <!-- 簡化版或行動版 -->
        <div class="flex-1 flex justify-between sm:hidden">
            @if ($paginator->onFirstPage())
                <span class="relative inline-flex items-center {{ $buttonSize }} rounded-nexus-md border border-nexus-gray-300 bg-white text-nexus-gray-500 cursor-not-allowed">
                    上一頁
                </span>
            @else
                <a href="{{ $paginator->previousPageUrl() }}" 
                   class="relative inline-flex items-center {{ $buttonSize }} rounded-nexus-md border border-nexus-gray-300 bg-white text-nexus-gray-700 hover:bg-nexus-gray-50 transition-colors">
                    上一頁
                </a>
            @endif

            @if ($paginator->hasMorePages())
                <a href="{{ $paginator->nextPageUrl() }}" 
                   class="relative inline-flex items-center {{ $buttonSize }} rounded-nexus-md border border-nexus-gray-300 bg-white text-nexus-gray-700 hover:bg-nexus-gray-50 transition-colors ml-3">
                    下一頁
                </a>
            @else
                <span class="relative inline-flex items-center {{ $buttonSize }} rounded-nexus-md border border-nexus-gray-300 bg-white text-nexus-gray-500 cursor-not-allowed ml-3">
                    下一頁
                </span>
            @endif
        </div>
    </nav>
@endif
```

### **第四天至第十天：繼續實作其他組件**

*[由於篇幅限制，其他天的詳細實作計劃將在後續文檔中完整展開，包括：]*

- **第四天**: 卡片、徽章、進度條組件
- **第五天**: 模態框、下拉選單、工具提示組件  
- **第六天**: 表格、列表、時間線組件
- **第七天**: 圖表整合、資料視覺化組件
- **第八天**: 檔案上傳、圖片裁切組件
- **第九天**: 主題切換機制與響應式優化
- **第十天**: 組件文檔、測試與驗收

---

## 🎯 **驗收標準**

### **功能驗收**
- [ ] 50+ UI組件完整實作
- [ ] 主題切換無縫運作
- [ ] 響應式設計完美適配
- [ ] 組件可重用性 100%

### **品質驗收**  
- [ ] 程式碼通過 ESLint/StyleLint 檢查
- [ ] 組件載入時間 < 100ms
- [ ] 無障礙設計符合 WCAG 2.1 AA
- [ ] 瀏覽器相容性 > 95%

### **文檔驗收**
- [ ] 每個組件都有使用範例
- [ ] API 文檔完整準確
- [ ] 設計指南清晰明確
- [ ] 故障排除說明完整

**下一階段**: P1.2 多步驟表單系統開發