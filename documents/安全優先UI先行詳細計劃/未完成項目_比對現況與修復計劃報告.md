## 未完成項目比對報告（現況 vs 規劃 vs 程式碼）

來源文件
- 目錄：`documents/安全優先UI先行詳細計劃/`（含 P1~P3 規劃與多份詳細實作/修復報告）
- 清單：`未完成項目整合清單.md`
- 參考：`CLAUDE.md`（流程與規範、任務管理、既有技術債記錄）

本次比對依「規劃 → 目前實作 → 缺口 → 修復建議」列出，並附上優先度與影響面。

---

### P1（高優先）

1) 設計系統與 UI 組件庫（缺口 ≈60%）
- 現況：
  - Blade + Tailwind 已使用，但缺少系統化 Design Tokens（顏色/字體/間距/陰影/Radius）與可重用元件庫。
  - 主題切換與變數命名不一致（`--nexus-*/--nx-*`），圖表/主題初始化在部分頁面仍有偶發問題。
- 缺口：
  - Tokens 定義、元件目錄結構、Story/Docs、可組合表單/列表/抽屜/Modal/Alert 等基礎元件。
- 修復：
  - 建立 `resources/css/tokens.css` 與 `@layer components` 統一定義；抽離通用元件到 `resources/views/components/`。
  - 新增 Story 檔與用法（以 Blade Partial 或簡易 Docs）。
  - 統一主題變數前綴 `--nexus-*`，修正圖表初始化順序。

2) 多步驟表單（缺口 ≈40%）
- 現況：
  - 已有 `/quotes/create/multi-step` 與單頁 `/quotes/create`。流程可用，但狀態/產品選擇/暫存一致性不足。
- 缺口：
  - 步驟間狀態持久化、產品搜尋與價格計算、重整保留、進度條/驗證提示一致性。
- 修復：
  - 導入統一表單狀態（localStorage/Session + 伺服器草稿 ID），產品選擇採 Debounce + 統一 API，補齊金額自動計算與驗證提示。

3) 儀表板（缺口 ≈50%）
- 現況：
  - 統計卡/圖表樣板存在，但數據串接、主題/初始化偶發不一致，缺即時更新。
- 修復：
  - 封裝 Chart 初始化 Helper；統一主題 Token；引入輪詢或 SSE（可先輪詢，後續 SSE）。

4) 導航與 UX（缺口 ≈50%）
- 現況：
  - 多層導覽、全域搜尋、麵包屑/行動版底部導航未完整；列表頁下拉定位已修正。
- 修復：
  - 建立 `layouts/partials/nav/*`，實作多層選單、全域搜尋框、麵包屑；行動版底部導航（重要操作）。

---

### P2（中優先）

1) API 安全補完（缺口 ≈30%）
- 現況：
  - 開發用 Base64 Token（`QuoteController::getOrCreateApiToken`）仍存在，未換 JWT；CORS、API Key、審計/版本控管未完整。
- 修復：
  - 改 JWT（Laravel 端簽發/Go 端驗簽）；設定嚴格 CORS；導入 API Key 管理（公司/應用維度）。
  - 建立存取日誌與審計表；規劃 `/v1` 版本前綴與棄用策略。

2) DEMO 場景（缺口 ≈70%）
- 現況：
  - 分散示例不少，但未形成 4 個完整情境（詢價/VIP 採購/庫存警示/財務分析）。
- 修復：
  - 為每個情境建立路徑、假資料、引導與自動播放腳本。

3) Mock 數據（缺口 ≈60%）
- 現況：
  - 有 seed/fixtures，但覆蓋不完整；跨表關聯與重置腳本不足。
- 修復：
  - 撰寫高擬真 Mock 產生器；提供一鍵重置腳本與多租戶樣本。

---

### P3（低優先）

1) 高級搜尋與篩選（缺口 ≈50%）
- 現況：
  - 報價列表已修正基本搜尋/排序參數映射，Go 後端已支援 `search/company_id/customer_name` JOIN，但尚未提供進階條件組合 UI。
- 修復：
  - 封裝前端搜尋條件（多欄位/區間/保存條件），後端擴充分頁/排序白名單與索引。

2) 報表與分析（缺口 ≈60%）
- 現況：
  - 圖表/統計存在，尚缺多數商務報表與匯出。
- 修復：
  - 新增銷售/庫存/客戶報表 API 與前端頁；CSV/XLSX 匯出。

3) 系統偏好（缺口 ≈70%）
- 現況：
  - 偏好/通知/語言/快捷鍵尚未成系統。
- 修復：
  - 偏好表與 UI，通知頻率設定，i18n 結構，快捷鍵表。

---

### 已知技術缺口與待辦（跨模組）

- 報價→訂單轉換：
  - 修復完成：後端加入 `company_id`（從客戶帶入）並寫入 `sales_orders`；需重建/重啟後端使修改生效。
  - 待辦：補單元/整合測試，驗證過期/已轉換/未批准情境訊息。

- 狀態枚舉不一致：
  - 前端 `sent/accepted` vs 後端 `pending/approved`，建立/更新已做映射；需全系統統一定義與 UI 顯示。

- 產品名稱載入：
  - 編輯頁已改 `normalizeQuoteData` 與多來源回退；中期後端扁平提供 `product_name`；`getQuoteItems` N+1 待批次化（已在 CreateQuote 用單查驗產品）。

- 搜尋/排序參數對齊：
  - 前端改 `page_size/sort_by/sort_order`，後端支援 `customer_name` 排序；需補索引與更多欄位白名單。

- 安全性：
  - Base64 Token 僅開發用，需改 JWT；補 CORS、API Key、審計、Rate limit 與防重放（Nonce/Timestamp）。

- 多租戶/RLS：
  - 前端已在列表查詢加 `company_id`；需全面檢查所有讀寫路徑；補齊 RLS Policy 覆蓋率清單。

- 測試：
  - 單元/整合/E2E 覆蓋率不足；需針對報價建立/更新/轉換、新 UI 元件、搜尋條件作完整測試。

- 可用性：
  - Flash 提示已改 10 秒並提高 z-index；需套用至其他關鍵頁（show/edit/訂單）。

---

### 建議實施順序（2 週衝刺）

Sprint Day 1-3
- 導入 JWT；設定嚴格 CORS；API Key 管理 MVP；審計表與中介層。
- 後端 CompanyID 修復後重建部署；補轉換流程測試。

Day 4-7
- 設計 Tokens 與元件庫（Button/Input/Select/Modal/Alert/Card/Dropdown/FormRow）+ 文檔。
- 多步驟表單狀態模型與產品搜尋/金額計算統一。

Day 8-10
- 儀表板卡片/圖表封裝與主題統一；新增 2 個報表頁（銷售/庫存）。
- 進階搜尋 UI 原型（條件儲存）。

Day 11-14
- DEMO 場景 1-2；Mock 生成與重置；E2E 覆蓋報價→訂單→發貨核心路徑。

---

### 子任務拆解（可直接建立任務）

- **P1.1 設計系統與 UI 組件庫**
  - 建立 `resources/css/design-tokens.css` 與 `@layer components`（顏色/字體/間距/陰影/Radius）
  - 統一變數前綴為 `--nexus-*`，移除/映射 `--nx-*`
  - 抽離並完成元件：`Button/Input/Select/Modal/Alert/Card/Dropdown/FormRow/Pagination`
  - 建立範例與用法文件（Blade 範例頁 `components/*`）
  - 封裝 Chart 初始化 Helper，修正主題切換時的重渲染

  進度更新（已完成）
  - 已存在並載入 `resources/css/design-tokens.css`，於 `resources/css/app.css` 先於 Tailwind 匯入
  - 新增 UI 元件：`Card`、`Alert`、`Modal`、`FormRow`（`resources/views/components/ui/`）
  - 新增範例頁：`resources/views/components/demo.blade.php`，路由 `/components/demo`
  - 補強：`Dropdown` 已 tokens 化，範例頁加入展示；建立 `resources/js/chart-manager.js`，支援主題切換時自動重繪
  - 測試：PHP 語法檢查通過；啟動本機伺服器後以 `GET /components/demo` 回應 200 並可視覺驗證（Modal/Dropdown 可互動）
  - 待辦：Pagination 視覺一致性微調與範例、實際頁面替換舊實作、Chart helper 串接到實際儀表板頁

- **P1.2 多步驟表單（報價）**
  - 統一狀態映射（sent→pending、accepted→approved），清理前端值
  - 表單狀態持久化（localStorage + 伺服器草稿 ID 回寫）
  - 產品搜尋 API 與前端 debounce 查詢、選擇寫回 `product_id`
  - 金額自動計算（含折扣/稅）、欄位驗證提示一致化
  - 建立 E2E 測試：單步/多步建立、重整不丟失、提交成功

- **P1.3 儀表板**
  - 封裝 `ChartManager`（註冊、重繪、主題切換）
  - 取代頁面內零散初始化，統一入口
  - 即時更新（先輪詢，再規劃 SSE）
  - 視覺一致性調整與單元測試

  進度更新（已完成）
  - 已新增 `resources/js/chart-manager.js` 並在 `resources/js/app.js` 載入為 `window.Charts`
  - `resources/views/dashboard/index.blade.php` 改為以 `window.Charts.create(...)` 初始化三個圖表，移除 CDN 依賴
  - `resources/js/components/dashboard/DashboardManager.js` 已存在並負責自動輪詢、事件派發與 DOM 狀態切換
  - 測試：`/dashboard` 需登入（302 正常）；`/components/demo` 200 並可見樣式與互動；Vite 解析 `chart.js` 依賴正常

- **P1.4 導航與 UX**
  - 多層級導航（桌面/行動）元件化，落地到 `layouts`
  - 麵包屑組件，支援動態路徑
  - 全域搜尋框（關鍵字 → 導航到列表並帶參數）
  - 行動版底部導航（常用操作）
  - 導航使用分析（事件蒐集、熱區）

  進度與問題（待修復）
  - 現況：`enhanced-navigation.blade.php` 已導入使用者選單與事件，`navigation-functions.js` 已載入，Console 無錯誤。
  - 問題：
    - 使用者資訊下拉選單在深色主題下背景仍呈現透明（看不到選項）。
    - 選單預設載入時偶發開啟（應該僅在點擊後開啟）。
  - 研判：Blade 內下拉樣式與 `navigation-functions.js` 的修復程式（`applyDropdownFixes/force styles`）存在衝突；`x-bind:style` 與強制樣式（backdrop-filter/opacity/visibility）互相覆蓋。
  - 待辦修復方案：
    - 移除所有會改動 `display/visibility/opacity` 的強制樣式注入，顯示僅由 `x-show + x-cloak` 控制。
    - 將下拉選單背景改由 CSS 規則固定：`.nexus-user-dropdown-modern{background:#fff}.dark .nexus-user-dropdown-modern{background:#1f2937;border:2px solid rgba(55,65,81,.8)}`，移除 `x-bind:style`。
    - 確認 `navigation-functions.js` 僅做結構與文字方向修復，不覆蓋背景/顯示屬性。
    - 事件綁定統一使用 `scheduleCloseUserMenu/cancelCloseUserMenu`，避免多重 timeout。
  - 驗證：
    - 進頁不自動開啟；點擊頭像才開啟；深色主題背景不透明；切換主題後背景維持正確。

- **P2.1 API 安全補完**
  - Laravel 簽發 JWT（替代 Base64 Token），Go 驗簽中介層
  - 嚴格 CORS 設定與白名單
  - API Key 管理（模型/中介層/管理頁）
  - 存取審計表（路由/使用者/公司/耗時/結果）
  - API 版本前綴 `/v1` 與棄用策略頁
  
  進度更新（本階段跳過）
  - 依產品決策：P2.1 於本衝刺暫不處理，狀態標註「跳過」。後續另行排期執行（不影響 P2.2 進行）。

- **P2.2 DEMO 場景**
  - 新客戶詢價：路徑、假資料、引導步驟
  - VIP 採購：定價/折扣示例
  - 庫存警示：報表與提醒流程
  - 財務分析：關鍵指標圖卡
  - 自動播放腳本與切換動畫
  
  進度更新（啟動）
  - 已建立 DEMO 線路總覽與四個情境頁面骨架：`/demo`、`/demo/inquiry`、`/demo/vip-procurement`、`/demo/inventory-alert`、`/demo/finance-analysis`。
  - 目前以靜態假資料與引導說明為主，後續串接實際 API 與自動播放腳本。

  教學（將搬至 README）
  1. 重置並產生 DEMO 資料（本機）：
     - 介面：進入 `GET /demo`，點擊「重置 DEMO 假資料（本機）」；
     - 或 CLI：`php artisan demo:seed --reset --orders=15 --products=30 --quotes=10 --with-inventory --warehouses=2 --low=20`。
  2. 四大情境：
     - 新客戶詢價：`/demo/inquiry` → 檢視假資料，點「開始多步驟建立」導向 `/quotes/create/multi-step`；
     - VIP 採購：`/demo/vip-procurement` → 顯示等級與價格清單；
     - 庫存警示：`/demo/inventory-alert` → 顯示低庫存表；
     - 財務分析：`/demo/finance-analysis` → 顯示 KPI 與趨勢圖（使用 `window.Charts`）。
  3. 快捷指令：
     - 只產生庫存：`php artisan mock:inventory --reset --warehouses=3 --products=50 --low=25`；
     - 只產生銷售資料：`php artisan demo:seed --reset --orders=20 --products=40`；
     - 產生報價（需 Go 後端）：`php artisan demo:seed --quotes=10`。

- **P2.3 Mock 數據**
  進度更新（已完成）
  - 新增 Artisan 指令：
    - `demo:seed`：支援 `--reset --orders --products --customers --quotes --with-inventory --warehouses --low`，可同時產生銷售、報價（Go Backend）與庫存 Mock。
    - `mock:inventory`：產生倉庫/產品/庫存水準，支援低庫存比例控制。
  - `/demo/reset`：一鍵重置並重新生成（呼叫 `demo:seed`），完成後以 Toast 顯示統計摘要。
  - `GET /demo/summary`：提供 JSON 統計（customers/products/orders/order_items/inventory/low_stock/quotes），`/demo` 首頁以卡片顯示並自動載入。
  - 修正 `finance-analysis` 的 JSON 內嵌語法，改用 `json_encode` 指派避免括號不匹配。

- **P3.1 進階搜尋與篩選**
  進度更新（已完成第一階段）
  - 報價列表加入「條件保存/套用」UI（localStorage + 後端保存），支持刪除；快速條件徽章（⭐預設置頂，最多 3 筆）。
  - 後端保存改為公司層級共用：`quote_filter_presets`（含 `shared/is_default`），API：`GET/POST/DELETE /api/quotes/filter-presets`。
  - 下一步：白名單/索引檢查與跨頁同步（必要時再補）。

- **P3.2 報表與分析**
  - 銷售/庫存/客戶報表 API 與頁面
  - 匯出 CSV/XLSX 與進度提示
  - 報表樣式統一與測試
  
  進度更新（進行中 → 部分完成）
  - 已完成：
    - 銷售總覽 `/reports/sales/summary`：改對接 DB 統計 API（`/api/reports/sales`）；修正圖表欄位鍵（`total_sales`/`total_spent`）；新增「下載 CSV（明細）」與「下載趨勢圖 PNG」。
    - 客戶銷售分析 `/reports/sales/by-customer`：對接 `/api/reports/sales/by-customer`；新增「匯出 CSV」與「下載圖表 PNG」。
    - 庫存估值 `/reports/inventory/valuation`：改由 `/api/inventory/levels` 取數，彙總各分類「成本估值 vs 市場估值」；新增「下載分佈 PNG」與「下載對比 PNG」。
    - 報表中心 `/reports/`：卡片調整為 5 欄等高，按鈕色彩統一（`nx-btn-*`）。
  - 待辦（下一步）：
    - 報表公司切換與公司層級快取；日期/公司切換的錯誤提示與 loading skeleton。
    - 銷售總覽/庫存估值加上 PNG/CSV 的服務端匯出（目前為前端匯出）。
    - 新增 Inventory/Customers 其他報表頁骨架與串接（估價明細 CSV、庫存週轉/老化、客戶 LTV/留存）。

- **P3.3 系統偏好**
  - 偏好表/後端 API 與前端設定頁
  - 通知頻率/語言/i18n 架構/快捷鍵設定

- **跨模組技術任務**
  - 報價→訂單轉換：company_id 寫入測試（approved/已轉換/過期）
  - 狀態枚舉全域統一與 UI 顯示一致化
  - 後端回傳扁平 `product_name`；`getQuoteItems` 改批次查詢
  - Flash 提示樣式與 10 秒顯示套用至 show/edit/訂單頁
  - RLS Policy 覆蓋檢查清單與補齊
  - 單元/整合/E2E 覆蓋率提至 ≥80%（核心模組）

---

### 驗證清單（完成定義）
- 報價→訂單可於 approved 狀態一次成功轉換，並在 `sales_orders.company_id` 正確寫入。
- 報價列表搜尋：關鍵字/日期/狀態/客戶名皆正確，排序能以 `customer_name/quote_date/total_amount` 生效。
- 多步驟表單：重整不丟失、金額自動計算、驗證提示一致。
- 設計系統：Tokens 文件、組件清單、範例頁、主題切換、圖表主題一致。
- 安全：JWT 生效、CORS 白名單、API Key 管理、審計紀錄落 DB。
- 測試：關鍵模組單元 ≥80%，E2E 路徑綠燈。

---

附註
- 本報告以「文件/程式碼現況」與「近期修復提交」為基準；若後端尚未重新部署，請先重建後端以載入最新修復（`sales_orders.company_id` 寫入）。

